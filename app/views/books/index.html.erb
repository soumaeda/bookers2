  <div class="col-md-8 offset-md-1">
    <h2>Books</h2>
    <table>
      <% @books.each do |book| %>
        <tr>
          <th>Title</th>
          <th><%= book.title %></th>
        </tr>
        <tr>
          <th>Opinion</th>
          <th><%= book.body %></th>
        </tr>
      <% end %>
    </table>
  </div>